# CRUD App (Demo)

A minimal Node.js Express CRUD demo app with an in-memory store.

Endpoints:
- GET /items
- GET /items/:id
- POST /items
- PUT /items/:id
- DELETE /items/:id

To run:

```powershell
npm install
npm start
```

Use `curl` or a tool like Postman to hit endpoints.
